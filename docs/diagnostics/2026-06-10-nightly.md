# Nightly Diagnostics — 2026-06-10

**Overall status: 🟢 GREEN** — both mechanical gates pass clean. One honesty finding
worth a look (a demo-mode AI response with a fabricated dollar figure that isn't
prod-gated), and `npm audit` still needs a networked machine. No regressions vs. the
2026-06-09 nightly.

## TL;DR (prioritized)
- 🟡 **Honesty risk:** `api/insights/chat` demo mode returns a canned, confident tax
  figure ("save you **$23,100**… Solo 401(k)") whenever `ANTHROPIC_API_KEY` is unset —
  and it is **not** gated to non-prod. If the key is missing in prod, real users see a
  fabricated number. Gate it or strip the figure. *(See Honesty pass.)*
- ✅ **Gates green:** `eslint` and `tsc --noEmit` both exit 0, clean. Install + migration
  have clearly been run here, so the usual "expected pending" Prisma/module caveats do
  **not** apply tonight — these are genuine passes.
- 🟠 **`npm audit` not run** — blocked by the network allowlist (403). Still an open
  critical-path launch item; run on a networked machine before go-live.
- ⚪ **Git hygiene N/A** — `/Users/eric/markup-ai` is not a git repo in this environment
  (no `.git`), so uncommitted/untracked changes couldn't be assessed here.
- ⚪ **Minor cleanup:** `src/lib/mock-cpa-data.ts` exists but is **not imported anywhere**
  (dead code, no leak); a stray `.env.save` backup sits in the root. Both safe, both
  worth deleting.

## Gates
| Gate | Result | Notes |
|------|--------|-------|
| `npx eslint .` | ✅ PASS | Exit 0, no warnings/errors. |
| `npx tsc --noEmit` | ✅ PASS | Exit 0, clean. node_modules + Prisma client present, so no pending-install/migration errors. |
| `npm audit --omit=dev` | ⚠️ BLOCKED | 403 from registry — network allowlist. Not run. |

## Security / Deps
- **Audit:** could not run (network). Severity counts unknown — **must run on a networked
  machine**; per OPS-CHK-001 §A, critical vulns block launch.
- **Secret scan (src/):** no hardcoded live secrets, API keys, or private keys found.
  `.gitignore` covers `.env*`; `.env`/`.env.example`/`.env.save` are local-only.
- **Demo login backdoor:** correctly gated to `NODE_ENV !== 'production'`
  (`api/auth/login`) — matches the readiness "demo backdoor disabled in prod" claim. ✅

## Honesty pass (security-legal lens)
- 🟡 **`src/app/api/insights/chat/route.ts:81-92`** — demo fallback (triggered when
  `ANTHROPIC_API_KEY` is absent) streams a fixed message containing a specific,
  authoritative-sounding claim: *"setting up a Solo 401(k) before Dec 31, which could save
  you $23,100 in taxes this year alone."* Unlike the demo login, this path has **no
  prod guard** — a missing/expired key in production would surface a fabricated tax
  figure to real users. **Recommend:** gate to non-prod (or fail closed) and replace with
  a neutral "live AI not configured" message with no dollar amount.
- ✅ **`src/lib/mock-cpa-data.ts`** — full set of mock tax/deduction/entity figures, but
  grep confirms **zero imports** across `src/`. No active leak; safe to delete.
- ✅ Everywhere else, the "demo" hits are honest *anti*-demo comments — P&L, KPIs, CPA,
  metrics, forecasting all explicitly state they never show demo data and use connect-prompts.
  This is consistent with policy.
- (security-legal-specialist subagent not invoked — findings above are clear-cut; flag for
  a deeper read if desired.)

## Launch-readiness gaps (OPS-CHK-001, updated Jun 9)
Open items by section — **critical-path flagged 🔴**:

- **A. App security:** 🔴 security headers (HSTS/CSP/etc. — none set; policy falsely claims
  HSTS), 🔴 CORS fix (`ACAO: *` + credentials is invalid), 🔴 rate limiting on
  auth/MFA/register, 🔴 `npm audit` clean.
- **B. Observability:** error tracking (Sentry), CloudWatch alarms→SNS, external uptime probe.
- **C. Reliability:** ECS autoscaling, cron batching/queue, [you] Neon backups/PITR, graceful
  shutdown check.
- **D. Infra:** 🔴 WAF on ALB, [you] GuardDuty+CloudTrail, [you] ACM cert, [you] Terraform
  state backend (S3+DynamoDB), secrets rotation.
- **E. Compliance:** [you] diligence questionnaire facts, [you] confirm background checks +
  pen test actually performed (policy now asserts them), cookie banner if EU/UK.
- **F. Product:** [you] billing decision (Stripe/Plan exist, flow unwired), transactional
  email (deletion confirmation promised in policy §5.2 — no provider wired), onboarding
  review, [you] support channel.
- **G. Final QA:** end-to-end prod test (register→MFA→bank→dashboard→delete), load test,
  full green build on deploy target.

## Recent decisions (docs/decisions/)
- **0035** (Jun 9) — Sign in/up with Google, passkey, and enterprise SSO (WorkOS); all
  converge on the same session, app TOTP/passkey still gates Plaid.
- **0033** (Jun 9) — Passkeys (WebAuthn) as a second factor; ≥1 passkey satisfies 2FA.
  ⚠️ note: backup codes remain non-functional (shown, never stored) — implement or remove.
- **0032** (Jun 9) — Deploy target: AWS ECS Fargate.
- **0031** (Jun 9) — Production hardening for go-live.

## Recommended next actions
1. **Decide the demo-AI figure** — gate `insights/chat` demo mode out of prod or strip the
   $-amount (fastest honesty win; trivial code change, deferred per read-only policy).
2. **Run `npm audit --omit=dev` on a networked machine** — last unknown in OPS-CHK-001 §A;
   triage any critical/high before launch.
3. **Knock out the §A code criticals** — security headers, CORS lock-down, rate limiting.
   All code, buildable now, all 🔴 critical-path.
4. **Resolve backup-codes** (per 0033) — either real hashed recovery codes or remove the UI.
5. **Cleanup:** delete unused `src/lib/mock-cpa-data.ts` and stray `.env.save`.

---
*Read-only run. No code changed. Gates executed in the Linux sandbox; `npm audit` and git
status could not run here (network allowlist / no repo) — noted above.*
