# Naviio — Nightly Diagnostics, 2026-06-17

*Read-only diagnostic run. No code changes made.*

## TL;DR
1. **All quality gates green** — `eslint` clean, `tsc --noEmit` clean (exit 0, no errors — Prisma/install state on this machine is fine; none of the known-expected pending-migration errors appeared).
2. **Doc-vs-code mismatch, in your favor:** the pre-launch checklist (OPS-CHK-001, last updated Jun 9) still lists **security headers, CORS, and rate limiting** as `[todo]`, but all three are **implemented in code today**. The checklist understates readiness — reconcile it.
3. **`npm audit` could not run** — blocked by the network allowlist (403 from registry). Still the one open critical-path item in Section A; run it on a networked machine before launch.
4. **Working tree is clean** — 0 uncommitted or untracked files. 32 commits since the last nightly (Jun 11), almost all mobile-first UI redesign + transaction-categorization improvements.
5. **Honesty pass clean** — no hardcoded secrets in `src/`, demo login is correctly gated to non-production, and "mock/fake" string hits are all honesty *comments* ("NO mock data", "never faked"), not leaked figures.

## Gates
| Gate | Result | Notes |
|---|---|---|
| `npx eslint .` | ✅ PASS | exit 0, no warnings/errors |
| `npx tsc --noEmit` | ✅ PASS | exit 0, fully clean |

No regressions. The known-expected tsc errors (Prisma `Credential`/`deletedAt`/`newAccountsAvailable`, `@simplewebauthn`/`@workos-inc`) did **not** appear, meaning install + migration state is good on this machine.

## Security / Deps
- **`npm audit --omit=dev`: NOT RUN** — `403 Forbidden`, connection blocked by network allowlist. This is an environment limitation, not a code finding. Must be run on a networked machine; per the checklist, critical vulns block launch.
- **No hardcoded secrets** found in `src/` (no live keys, embedded passwords, or non-env credential literals).
- **Demo backdoor correctly gated:** `src/app/api/auth/login/route.ts` sets `DEMO_LOGIN_ALLOWED = NODE_ENV !== 'production'`; in prod the branch is dead and demo creds fall through to normal (failing) auth. Consistent with the Plaid MFA attestation.
- **Rate limiting is live** (`src/lib/rate-limit.ts`, Redis-backed with in-memory dev fallback, fail-open): covers `login`, `register`, `mfa/verify`, `waitlist`, and `portal`.
- **Security headers are set** (`next.config.ts`): HSTS, CSP, X-Frame-Options DENY, X-Content-Type-Options, Referrer-Policy, Permissions-Policy.
- **CORS is correct:** no permissive `*`; production emits no cross-origin headers (same-origin app), dev tunnel is locked to the app origin with no `*`-with-credentials.
- *Note:* the `security-legal-specialist` subagent referenced in the task is not available in this environment, so the honesty pass was done directly via code scan.

## Launch-readiness gaps (OPS-CHK-001)
**⚠️ The checklist is stale (last updated Jun 9).** Several Section A items it marks `[todo]` are now done in code — verify and re-mark:

**A. Application security — mostly resolved in code now**
- ~~Security headers~~ → **appears DONE** (next.config.ts).
- ~~Fix CORS~~ → **appears DONE** (no `*`; prod emits none).
- ~~Rate limiting~~ → **appears DONE** (Redis-backed, all sensitive routes).
- **`npm audit` clean** → still open (couldn't run here; **critical path**).

**B. Observability & ops** (all open) — error tracking (Sentry), CloudWatch alarms→SNS, external uptime probe on `/api/health`.

**C. Reliability / scale** (all open) — ECS autoscaling, cron-sync batching/queue, **[you]** Neon backups/PITR confirmation, graceful-shutdown check.

**D. AWS infra hardening** (foundation done) — **WAF** on ALB, **[you]** GuardDuty + CloudTrail, **[you]** ACM cert, **[you]** Terraform state backend (S3+DynamoDB), secrets rotation. *Critical-path infra, needs your accounts.*

**E. Compliance / legal** — **[you]** fill diligence facts (EIN/entity/headcount), **[you]** confirm background checks + pen test actually performed, cookie/consent banner if EU/UK.

**F. Product / business** — **[you]** billing decision (Stripe flow not wired), **transactional email** provider (deletion confirmation promised in policy §5.2 — *policy-vs-code gap, still open*), onboarding review, **[you]** support inbox.

**G. Final QA gate** — full E2E (register→MFA→bank→dashboard→delete), load test, build green.

**Critical path before first customer:** `npm audit` (A) → WAF/ACM/state-backend/GuardDuty (D, your accounts) → error tracking + alarms (B) → transactional email + billing decision (F) → production E2E QA (G).

## Recent decisions
Newest in `docs/decisions/` — recent work is overwhelmingly the mobile-first redesign:
- **0046** (Jun 16) — Mobile hero on financial pages (P&L, Cash Flow, Revenue, Expenses) via shared `MobileHero`; trend arrows only where a real period-over-period value exists (no fabricated trends).
- **0045** (Jun 16) — Mobile dashboard: one hero (Runway) + 3 source-aware chips; header reduced to brand icon + bell.
- **0044** (Jun 16) — Mobile-first redesign of auth + dashboard (responsive, desktop untouched via `lg:` resets).
- **0043** (Jun 16) — Mobile bottom tab bar.
- **0042** (Jun 15) — Functional header controls (search/notifications/refresh).
- **0041** (Jun 15) — Review fixes: rocks bundle, auth, a11y.

## Recommended next actions
1. **Run `npm audit --omit=dev` on a networked machine** — last unrun Section-A gate; triage any critical/high before launch.
2. **Reconcile OPS-CHK-001** — mark security headers, CORS, and rate limiting as done (verify against the live app first, esp. that Plaid Link still opens under the CSP). This materially shrinks the "critical path" list.
3. **Stand up the infra-hardening items (Section D)** — WAF, ACM, Terraform state backend, GuardDuty/CloudTrail. These need your AWS accounts and are now the largest remaining critical-path block.
4. **Wire transactional email** (at minimum the account-deletion confirmation promised in policy §5.2) — current policy-vs-code gap.
5. **Schedule the production E2E QA pass** (register→MFA→connect bank→dashboard→delete) once infra is up.
