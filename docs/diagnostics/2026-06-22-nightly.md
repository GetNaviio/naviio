# Naviio — Nightly Diagnostics, 2026-06-22

*Read-only diagnostic run. No code changes made. Deltas-focused; known backlog summarized, not restated.*

## TL;DR
1. **Status: GREEN.** Both code gates pass clean (`tsc` and `eslint`, exit 0, no output). No regressions vs. the Jun 19 run.
2. **Quiet 3 days — 6 commits, all fixes/UI**, centered on a **production login loop** (apex↔www `COOKIE_DOMAIN` fix) plus auth/Plaid error logging and a mobile icon tweak. No new features, no schema changes.
3. **Working tree is clean** — 0 uncommitted, 0 untracked. Nothing left dangling.
4. **Launch is still gated by the same critical-path items** (security headers, CORS fix, rate limiting, `npm audit`, WAF/ACM/state backend, error tracking, transactional email). None resolved this cycle — readiness doc unchanged since Jun 9.
5. **One thing to verify, not a defect:** a temporary Plaid debug-logging commit was added then properly reverted/gated to non-prod the same window — net state is safe, but confirm nothing verbose ships to prod.

## Gates
| Gate | Result | Notes |
|---|---|---|
| `npx tsc --noEmit` | ✅ PASS | Exit 0, fully clean. No Prisma/webauthn errors — install + migration state on target is good (not the "expected pending" case). |
| `npx eslint .` | ✅ PASS | Exit 0, zero warnings/errors. |
| `npm test` / `next build` | ⏭️ Not run | Skipped — same offline-sandbox SWC limitation as prior nights; exercise on a networked/native machine. |

## Security / Deps
- **`npm audit --omit=dev`: could not run** — blocked by the network allowlist (403 from registry). **Must be run on a networked machine** before launch (it's a critical-path checklist item, A.20). Carry-over from prior nights.
- **Honesty pass: clean.** Secret scan surfaced only env-driven, test-key-guarded Stripe code (`sk_test_`/`rk_test_` guards in `scripts/*.cjs` and `TempStripeKeyConnect.tsx`) — no hardcoded live secrets. All "demo/mock" hits in `src/` are *reassuring UI copy* ("never demo data", "no demo figures shown"), not figure leaks.
- **Watch (sandbox-only, not defects):** `TempStripeKeyConnect.tsx` temp sandbox-connect component is still present (env-gated); confirm it's gone before go-live. The Jun 20 `debug(plaid)` commit that surfaced link-token error detail was reverted by `b16e1f2` and gated to non-production — verify no other debug verbosity reaches prod responses.

## Launch-readiness gaps (OPS-CHK-001, last updated Jun 9 — unchanged)
Still-open items by section. **★ = critical path before first customer.**

- **A. App security (code):** ★ Security headers (HSTS/CSP/X-Frame/etc — none set, policy claims HSTS), ★ CORS fix (`Allow-Origin: *` with credentials is invalid), ★ rate limiting on auth routes, ★ `npm audit` clean.
- **B. Observability:** ★ error tracking (Sentry), alerting (CloudWatch→SNS), uptime probe on `/api/health`.
- **C. Reliability:** ECS autoscaling, cron batching/queue, [you] Neon backups/PITR, graceful shutdown check.
- **D. AWS infra:** ★ WAF on ALB, [you] GuardDuty + CloudTrail, [you] ACM cert, [you] Terraform state backend (S3+DynamoDB), secrets rotation.
- **E. Compliance:** [you] diligence questionnaire facts, [you] confirm background checks + pen test performed, cookie/consent banner (EU/UK).
- **F. Product/biz:** [you] billing decision (Stripe `Plan` exists, subscription flow not wired), ★ transactional email (deletion confirmation promised in policy §5.2), onboarding review, support inbox.
- **G. Final QA:** end-to-end prod test (register→MFA→connect bank→dashboard→disconnect→delete), load test, full green gate on prod.

No checklist items flipped to done since the last nightly.

## Recent decisions (newest in docs/decisions/)
- **0054 — Pricing architecture** (accepted, Jun 19): single source of truth tying together solo / multi-entity / firm pricing. Supersedes 0053, complements 0052.
- **0053 — Firm billing plans** (accepted, Jun 19): white-label vs. SaaS-resale firm models; sets per-org overage + included-org count + commission rate.
- **0052 — Advisor access + external doc sharing** (accepted, Jun 19): fractional-CFO multi-client access model and out-of-band sensitive-doc exchange.
- **0051 — Navi as a tool-using agent** (accepted, phases 1–3 shipped, Jun 18).

All recent decisions are the billing/pricing + advisor GTM cluster already shipped in the Jun 19 push; nothing new landed in the last 3 days.

## Recommended next actions
1. **Run `npm audit --omit=dev` on a networked machine** — only remaining unknown in the security picture; critical vulns would block launch.
2. **Knock out the A-section code work** (security headers, CORS lockdown, auth rate limiting) — pure code, buildable now, and the longest pole on the critical path.
3. **Decide transactional email + billing wiring** (F) — deletion-confirmation email is already promised in the published policy, so this is a compliance gap, not just a feature.
4. **Confirm sandbox artifacts are out of the prod path** before go-live: `TempStripeKeyConnect.tsx`, `.env.save`, and any remaining debug logging.
5. Infra (D) items are [you]/account-dependent — sequence WAF/ACM/state-backend/GuardDuty alongside the code work so they're not the last-minute blocker.
