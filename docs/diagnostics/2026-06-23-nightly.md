# Naviio — Nightly Diagnostics, 2026-06-23

*Read-only diagnostic run. No code changes made. Deltas vs. the Jun 22 nightly; known backlog summarized, not restated.*

## TL;DR
1. **Status: GREEN.** Both code gates pass clean — `tsc --noEmit` and `eslint .`, exit 0, no output. No regressions, and not the "expected pending install/migration" case: install + Prisma state on this target are good.
2. **Big productive cycle — 43 commits since the last nightly** (Jun 22 14:54 → Jun 23 03:09), a real sprint after the quiet stretch. Headlines: the **multi-industry metric engine + per-industry Navi-score bands**, the **fractional-CFO advisor home** (`/advisor` portfolio view), **onboarding account-type branching** (owner vs. advisor), and a wave of **accounting-grade fixes** (ratable revenue recognition, Stripe-fee gross→net, payout/deposit reconciliation, NRR/churn per-customer).
3. **Working tree is clean** — 0 uncommitted, 0 untracked. Nothing left dangling. (Note: a stale `.git/index.lock` can't be unlinked in this sandbox; read-only status via `--no-optional-locks` confirms clean.)
4. **Launch readiness unchanged (OPS-CHK-001, still dated Jun 9).** None of the critical-path items resolved this cycle. The doc is now ~2 weeks behind a large new feature surface (advisor portfolio API, RBAC tiers, billing-as-source-of-truth) — **it should be refreshed**, both to re-scope and because new auth-sensitive paths landed.
5. **Honesty/security pass: clean.** No hardcoded secrets, no new demo/mock figure leaks. Two carry-over watch items: `npm audit` still must run on a networked machine, and the temporary `TempStripeKeyConnect` sandbox control is still present (env-gated, self-marked for removal before launch).

## Gates
| Gate | Result | Notes |
|---|---|---|
| `npx tsc --noEmit` | ✅ PASS | Exit 0, fully clean. No Prisma `Credential`/`deletedAt`/`newAccountsAvailable` or `@simplewebauthn`/`@workos-inc` errors — install/migration state on target is good (not the "expected pending" case). |
| `npx eslint .` | ✅ PASS | Exit 0, zero warnings/errors. |
| `npm test` / `next build` | ⏭️ Not run | Same offline-sandbox limitation as prior nights; run on a networked/native machine. New unit tests added this cycle (`tests/lib/vitals.test.ts`, scoring/registry/industry specs) — exercise them there. |

## Security / Deps
- **`npm audit --omit=dev`: could not run** — blocked by the network allowlist (403 from registry.npmjs.org). **Must be run on a networked machine** before launch (critical-path item A.20). Carry-over from prior nights.
- **Secret scan: clean.** Only matches are env-driven, test-key-guarded Stripe code (`sk_test_`/`rk_test_` guards) — no hardcoded live secrets.
- **Demo/mock scan: clean.** All "demo/mock" hits in `src/` are *reassuring UI/code copy* ("never demo data", "no demo figures shown", connect-prompts instead of fake numbers) — not figure leaks. The login `DEMO_USER` backdoor remains correctly gated to `NODE_ENV !== 'production'`. The new `vitals.ts`/`/advisor` code computes from live per-client metrics with graceful `no_data`/`needs_reconnect` states — no placeholder numbers.
- **Watch (sandbox-only, not a defect):** `src/components/integrations/TempStripeKeyConnect.tsx` is still present. It's well-guarded — rendered only when `NEXT_PUBLIC_STRIPE_KEY_CONNECT === '1'`, refuses live keys client-side — and its own header says "DELETE THIS FILE when done." Confirm it (plus its import in `integrations/page.tsx` and the env var) is gone before go-live.

## Launch-readiness gaps (OPS-CHK-001, last updated Jun 9 — unchanged)
Still-open items by section. **★ = critical path before first customer.**

- **A. App security (code):** ★ Security headers (HSTS/CSP/X-Frame/X-Content-Type/Referrer/Permissions — none set; policy claims HSTS), ★ CORS fix (`Allow-Origin: *` with credentials is invalid), ★ rate limiting on `/api/auth/login` · `/mfa/verify` · `/register`, ★ `npm audit` clean.
- **B. Observability:** ★ error tracking (Sentry), alerting (CloudWatch→SNS), uptime probe on `/api/health`.
- **C. Reliability:** ECS autoscaling, cron batching/queue, [you] Neon backups/PITR, graceful-shutdown check.
- **D. AWS infra:** ★ WAF on ALB, [you] GuardDuty + CloudTrail, [you] ACM cert, [you] Terraform state backend (S3+DynamoDB lock), secrets rotation.
- **E. Compliance/legal:** [you] diligence questionnaire facts (EIN/entity/headcount), [you] confirm background checks + pen test actually performed, cookie/consent banner if EU/UK.
- **F. Product/business:** [you] billing decision — *note: this cycle moved billing toward "subscription is the single source of truth for plan" and made white-label firm-only, so the Stripe plumbing is maturing even though the readiness doc still lists subscription flow as not wired*; transactional email (deletion confirmation etc., none wired); onboarding review (*advanced materially this cycle — account-type branching + industry step*); [you] support inbox.
- **G. Final QA gate:** end-to-end register→MFA→bank→dashboard→delete, load test, full green `eslint`/`tsc`/`test`/`build`.

## Recent decisions (newest first)
- **0063 — Advisor home dashboard** (Jun 23 03:08): `/advisor` portfolio view for fractional CFOs. New aggregation API `GET /api/firm/clients/vitals` (per-client cash/runway/margin/growth/industry-graded score + triage), per-client access re-verified via `getRole`, live Plaid wrapped in a 5s timeout and run in parallel, cached per firm. Pure `deriveVitals` (`src/lib/firm/vitals.ts`), unit-tested. No new migration.
- **0062 — Onboarding: company name + business type up front** (Jun 22 02:51, + account-type branching update): register API now keeps the `company` field (was silently dropped) and names the org from it; onboarding wizard opens with "owner vs. fractional CFO" choice and a business-type step so metrics are industry-tailored from first impression. `User.accountType` via migration `20260622040000`.
- **0061 — Industry metric packs (Phase 3)** (Jun 23 00:26): metric-registry architecture — each metric declares applicable industries and a pure `compute(ctx)` returning `null` when inputs are absent; `selectMetrics` splits `visible` vs. `locked` ("connect X to unlock"), with honest graceful degradation (never a fake/$0 value).
- *Context:* 0056–0060 (Jun 22) covered the industry framework, gross-margin on live P&L, revenue-recognition/Stripe-fee bridge, ratable recognition, and churn/forecast reconciliation — the accounting-accuracy thread behind this cycle's commits.

## Recommended next actions
1. **Run `npm audit --omit=dev` on a networked machine** — only remaining unknown on the gates; it's a launch blocker if criticals surface.
2. **Refresh OPS-CHK-001 (Jun 9 → now).** A lot shipped since: advisor portfolio API, Partner/Analyst RBAC, billing-as-source-of-truth, public `/security` page. Re-scope open items and add the new auth-sensitive surfaces (especially the cross-org `/api/firm/clients/vitals` access path) to the QA gate.
3. **Still the top code blockers:** security headers, CORS lock-down, and auth-route rate limiting — none touched this cycle; they're the cheapest critical-path wins still entirely in-code.
4. **Pre-launch cleanup reminder:** delete `TempStripeKeyConnect` + its env var/import before go-live.
5. **Exercise the new tests + build on a real host** — this sprint added the advisor/vitals and industry-scoring suites that the sandbox can't run.
