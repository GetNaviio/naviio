# Nightly Diagnostics — 2026-06-11

**Overall status: 🟢 GREEN** — gates clean, and both honesty findings from the
2026-06-10 nightly are **fixed** (demo-AI fabricated figure removed; dead mock file
deleted). Big day yesterday: §A critical-path security items (headers, CORS, rate
limiting) shipped in code, but the readiness checklist hasn't been updated to say so.

## TL;DR (prioritized)
- ✅ **Gates green:** `eslint` and `tsc --noEmit` both exit 0, clean. Genuine passes —
  install/migrations present, so no "expected pending" caveats apply.
- ✅ **Yesterday's honesty findings resolved:** `insights/chat` demo mode now returns a
  neutral "demo mode" message with **no fabricated dollar figure**, and unused
  `src/lib/mock-cpa-data.ts` is deleted.
- 🟡 **Backup codes still cosmetic** (carried over from 0033): `mfa/enable` generates 8
  codes and shows them with "store in a password manager" copy, but they're **never
  persisted and no verify path accepts them**. Users are saving codes that won't work —
  implement (hashed) or remove the UI.
- 🟠 **`npm audit` still blocked** here (403, network allowlist). Last unknown in
  OPS-CHK-001 §A — run on a networked machine; criticals block launch.
- ⚪ **Checklist is stale (good news variety):** OPS-CHK-001 (last updated Jun 9) still
  lists security headers, CORS, and rate limiting as 🔴 todo, but all three shipped
  Jun 10 (`next.config.ts`, `src/lib/rate-limit.ts`). Update it so the critical path
  reflects reality.

## Gates
| Gate | Result | Notes |
|------|--------|-------|
| `npx eslint .` | ✅ PASS | Exit 0, no warnings/errors. |
| `npx tsc --noEmit` | ✅ PASS | Exit 0, clean. No Prisma/`@simplewebauthn`/`@workos-inc` pending-install errors — real pass. |
| `npm audit --omit=dev` | ⚠️ BLOCKED | 403 from registry (network allowlist). Not run. |
| `git status` | ⚪ N/A | No `.git` in this environment — uncommitted/untracked state couldn't be assessed. |

## Security / Deps
- **Audit:** blocked by network — severity counts unknown. Per OPS-CHK-001 §A this is
  critical-path; run `npm audit --omit=dev` on a networked machine.
- **Secret scan:** no hardcoded live keys/private keys in `src/`, `prisma/`, `scripts/`,
  `infrastructure/` (hits are prefix-format checks like `sk_test_…`, safe).
  `.env.save` backup still sits in the repo root — delete it.
- **New since Jun 10 (from the changelog, spot-verified in code):** security headers
  (HSTS, CSP, X-Frame-Options) in `next.config.ts`; CORS locked to
  `APP_ORIGIN || NGROK_HOST || localhost` (no more `*`+credentials); Redis fixed-window
  rate limiting on login/register/MFA-verify/WebAuthn; WebAuthn UV enforcement; startup
  env validation that fails closed in prod.
- **Two config foot-guns worth a glance:** (1) CORS falls back to `NGROK_HOST`/localhost
  if `APP_ORIGIN` is unset in prod — make sure `APP_ORIGIN` is in the prod env checklist;
  (2) rate limiting is **fail-open** when Redis is down — acceptable trade-off, but know
  that a Redis outage silently removes brute-force protection.

## Honesty pass (security-legal lens)
- ✅ **Fixed:** `src/app/api/insights/chat/route.ts` demo fallback now says "Navi is in
  demo mode… never made-up numbers" — the $23,100 Solo-401(k) fabrication is gone.
- ✅ **Fixed:** `src/lib/mock-cpa-data.ts` removed entirely.
- 🟡 **Backup codes** (`src/app/api/auth/mfa/enable/route.ts:29-31` +
  `settings/page.tsx`): codes are generated and returned to the UI with confident copy
  ("Each backup code can only be used once… Naviio cannot recover these codes") but
  nothing stores them and no login path consumes them. This is a policy-vs-code mismatch
  shown directly to users. Highest-priority honesty item remaining.
- ✅ No demo/mock/placeholder figures in the new credits, billing, or financial-model
  code (`src/lib/credits`, `src/lib/model`, billing APIs).
- (security-legal-specialist subagent not invoked — findings clear-cut; the new
  credits/accrual code already got its own review per 0038.)

## Launch-readiness gaps (OPS-CHK-001, last updated Jun 9 — now partially stale)
Critical-path 🔴; **[stale-done]** = listed as todo but shipped Jun 10, verify + check off:

- **A. App security:** **[stale-done]** security headers · **[stale-done]** CORS fix ·
  **[stale-done]** rate limiting · 🔴 `npm audit` clean (still genuinely open).
- **B. Observability:** error tracking (Sentry), CloudWatch alarms→SNS, external uptime probe.
- **C. Reliability:** ECS autoscaling, cron batching (note: Jun 10 work batched sync
  *writes*, the single-invocation cron concern likely still stands), [you] Neon
  backups/PITR, graceful-shutdown check.
- **D. Infra:** 🔴 WAF on ALB, [you] GuardDuty+CloudTrail, [you] ACM cert, [you]
  Terraform state backend, secrets rotation.
- **E. Compliance:** [you] diligence questionnaire facts, [you] confirm background
  checks + pen test actually performed (policy asserts them), cookie banner if EU/UK.
- **F. Product:** [you] billing decision — note 0036 shipped Stripe Checkout *credit
  reloads*, which may partly answer this; transactional email (policy §5.2 promises
  deletion confirmation — no provider wired), onboarding review, [you] support channel.
- **G. Final QA:** end-to-end prod test, load test, full green build on deploy target
  (migrations workflow is now `db:migrate`/`db:deploy`, validated on PRs).

## Recent decisions (new since last nightly — 0035 was newest then)
- **0039** (Jun 10) — Financial Model page (`/model`): editable forward projection,
  live-formula Excel export + print-to-PDF.
- **0038** (Jun 10) — Credits concurrency/billing-correctness hardening: two real
  money bugs fixed (webhook reconciliation, metered-refresh refund path).
- **0037** (Jun 10) — Accrual/GAAP P&L pulled live from QuickBooks/Xero, shown beside
  cash-basis figures.
- **0036** (Jun 10) — Usage-based credits + metered features (Navi messages, Plaid
  refresh) + Stripe Checkout reload flow, priced at actual vendor rates.
- Also: large Jun 10 engineering pass (see `docs/CHANGELOG-2026-06-10.md`) — 3 DB
  migrations (Float→Decimal for money 👏), OAuth-callback/refresh refactors,
  pagination fixes that were silently truncating ledgers at 100 records.

## Recommended next actions
1. **Fix or remove backup codes** — the one user-facing honesty gap left; small,
   self-contained change (hash + store + accept at login, or drop the UI).
2. **Update OPS-CHK-001** — check off headers/CORS/rate-limiting (verify in a deployed
   env first), so the critical path shows what's actually left: audit + infra (D) + email.
3. **Run `npm audit --omit=dev` on a networked machine** — last §A unknown.
4. **Add `APP_ORIGIN` to the prod env-var checklist** (CORS silently falls back to
   ngrok/localhost without it).
5. **Cleanup:** delete `.env.save`.

---
*Read-only run; no code changed. eslint/tsc executed in the Linux sandbox; `npm audit`
blocked by network allowlist and git status unavailable (no `.git` here) — both noted.*

---

# Addendum — second nightly run, 2026-06-11 (~12:35)

A second scheduled run executed after the report above. Findings are consistent —
**status remains 🟢 GREEN**. This addendum adds what the first run didn't cover.

## Gates (re-run)

| Gate | Result | Notes |
|------|--------|-------|
| `npx tsc --noEmit` | ✅ PASS | Exit 0, verified both with and without incremental cache. No `.next/types` errors. |
| `npx eslint .` | ✅ PASS | Exit 0. |
| `npm test` (jest) | ⚠️ ENV-BLOCKED | Fails before any test executes: Next SWC binary for linux/arm64 missing in the sandbox ("Failed to load SWC binary for linux/arm64"). Environment issue, **not a code bug** — no test signal available here. |
| `npx next build` | ⚪ SKIPPED | Same missing SWC binary; no code-defect signal obtainable. |

## Targeted regression audit (5 known bug classes) — zero new findings

One read-only agent swept all 71 `/api` route handlers plus
`src/lib/{integrations,credits,metrics,model,sync}`:

| Class | New findings |
|-------|--------------|
| (a) Unauthenticated `/api` routes doing DB/Anthropic work | **0** — every route uses `withOrg`/`withAuth`/inline `requireAuth()` or the correct alternative guard (timing-safe `CRON_SECRET` for cron, signature verification for webhooks, CSRF state for OAuth callbacks, intentional pre-auth flows). |
| (b) Lowercase provider string vs `IntegrationProvider` enum | **0** — all Prisma `integration` queries use UPPERCASE enums; both lowercase→enum maps (`integrations/disconnect/route.ts:11-21`, `integrations/refreshToken.ts:166-170`) correct, incl. `ghl → GOHIGHLEVEL`. |
| (c) `userId` passed where `orgId` expected | **0** — all data paths thread `orgId` from `getDefaultOrgId(user.id)`. (Known naming smell, unchanged from baseline: `fetch*(userId)` params in xero/gusto/adp/ghl helpers actually receive an orgId.) |
| (d) Money-unit mixups (Stripe cents vs major units) | **0** — `/100` at every Stripe boundary (`stripe-map.ts:25`, `stripe.ts:140,225-226,279`); ratios and credit-pack `priceCents`→`unit_amount` correct. |
| (e) Hardcoded secrets in source | **0** — informational only (known, prod-gated): demo login `password123` (`api/auth/login/route.ts:19`) and JWT dev fallback (see below). |

## Additional newly-FIXED item verified (vs 2026-06-09 baseline)

- **Baseline Critical #2 — `JWT_SECRET` fallback — now fails closed in production**:
  `src/lib/auth.ts:14-21` `getJwtSecret()` throws if unset in production; the
  `'dev-secret-change-in-production'` default is reachable only outside prod.

## Still open (unchanged — delta note only)

- **`src/proxy.ts:4,27`** (baseline Critical #1): `PUBLIC_PATHS` includes `'/'` with
  `startsWith`, so the page-level redirect gate still never fires. Data APIs remain
  protected by `requireAuth()`.
- `.env.save` still in repo root.

---
*Addendum run: read-only, no code changed. 1 audit agent. Compared against
`2026-06-10-nightly.md` and `2026-06-09-full-audit.md`.*
