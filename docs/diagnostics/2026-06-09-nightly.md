# Nightly Diagnostics — 2026-06-09

**Overall status: 🟢 GREEN** — all mechanical gates pass (jest blocked only by an
offline environment issue, not a code defect) and the audit surfaced **no new or
regressed issues** vs. the `2026-06-09-full-audit.md` baseline.

This is the first nightly run; the baseline full audit (same date) is the
reference point. Only deltas are reported below — the full known-issues backlog is
not restated.

## Mechanical gates

| Gate | Result | Notes |
|------|--------|-------|
| `npx tsc --noEmit` | ✅ PASS | Clean (`.next/types` paths ignored per policy). |
| `npx eslint .` | ✅ PASS | Exit 0, no warnings or errors. |
| `npm test` (jest) | ⚠️ ENV | Fails to start: "Failed to load SWC binary for linux/arm64" — Next SWC native binary not installed in the offline sandbox. **Environment issue, not a code bug.** No test assertions ran. |
| `npx next build` | ⏭️ SKIPPED | Optional; same SWC/offline constraint would apply. Not treated as a code defect. |

## New / regressed issues since baseline

**None.** One read-only audit agent swept the known bug classes (unauthenticated
`/api` routes, Prisma provider enum casing, orgId-vs-userId, money-unit mixups,
hardcoded secrets). Every finding it surfaced is an **already-tracked baseline
item**, at the same file:line, with no worsening:

- `src/app/api/alerts/route.ts:26` — alert PATCH not `orgId`-scoped (cross-tenant
  IDOR). Already baseline 🟠 High. **Unchanged.**
- `src/lib/auth.ts:7` — `JWT_SECRET` hardcoded fallback (+ `setSessionCookie`
  `secure:false`). Already baseline 🔴 Critical #2 / 🟡 Medium. **Unchanged.**
- `src/app/api/auth/login/route.ts:7` — `DEMO_USER` hardcoded `password123`.
  Already baseline 🟢 Low ("DEMO_USER bypass should be env-gated"). **Unchanged.**
- Integration libs (`xero.ts`, `adp.ts`, `gusto.ts`, `ghl.ts`) declare a `userId`
  param that actually receives `orgId`. Resolves correctly; naming smell only.
  Already baseline 🟢 Low ("`fetch*Data(userId)` params actually receive orgId").
  **Unchanged.**

No findings in: Prisma provider enum casing (all UPPERCASE; `'ghl'`→`'GOHIGHLEVEL'`
mapping correct), money-unit handling (Stripe cents converted at the boundary; no
cents/major mixing), or newly hardcoded provider secrets (`sk_live`/`sk-ant-`/
`whsec_`/DB strings all read from `process.env`).

## Newly-fixed baseline issues

**None confirmed as net-new fixes.** The baseline's "patched tonight" auth fixes
(`insights/chat`, `forecast`, `forecast/scenarios` now calling `requireAuth()`)
remain in place — the audit confirmed these routes are guarded — but these were
already recorded as fixed locally in the baseline, so they are not a delta for this
run. (Reminder from baseline: those local patches close the leak only after a
production deploy.)

## Bottom line

Code surface is stable since the baseline. The only red flags remain the
pre-existing Critical/High items (middleware `proxy.ts` auth gate, `JWT_SECRET`
fallback, P&L double-count, Stripe-metrics tenancy, alerts IDOR, CORS) — none of
which are new tonight. Nightly gate status is GREEN.
